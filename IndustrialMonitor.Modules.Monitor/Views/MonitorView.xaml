<UserControl x:Class="IndustrialMonitor.Modules.Monitor.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IndustrialMonitor.Modules.Monitor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <WrapPanel Margin="5">
                <TextBlock Text="设备地址" Margin="10,5" Width="auto"/>
                <TextBlock Text="从站 ID" Margin="10,5" Width="auto"/>
                <TextBlock Text="警告类型" Margin="10,5" Width="auto"/>
                <TextBlock Text="警告等级" Margin="10,5" Width="auto"/>
                <TextBlock Text="警告代码" Margin="10,5" Width="auto"/>
                <TextBlock Text="错误信息" Margin="10,5" Width="auto"/>
                <TextBlock Text="发布时间" Margin="10,5" Width="auto"/>
                <TextBlock Text="目前状态" Margin="10,5" Width="auto"/>
                <TextBlock Text="处理时间" Margin="10,5" Width="auto"/>
            </WrapPanel>

        </Grid>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding AlarmList}">

            <ItemsControl.ItemTemplate>
                <ItemContainerTemplate>
                    <DataGrid></DataGrid>
                </ItemContainerTemplate>

            </ItemsControl.ItemTemplate>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel>
                        
                    </WrapPanel>
                </ItemsPanelTemplate>

            </ItemsControl.ItemsPanel>

        </ItemsControl>
    </Grid>
</UserControl>
