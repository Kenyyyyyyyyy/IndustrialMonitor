<UserControl x:Class="IndustrialMonitor.Modules.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IndustrialMonitor.Modules.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>



        <TextBlock Grid.Row="0" Text="注塑设备实时监控看板" FontSize="20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock Text="今日产量" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding TodayOutput}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="件" FontSize="12" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="良品率" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding YieldRate}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="%" FontSize="12" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                <TextBlock Text="当前报警" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding AlarmCount}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="件" FontSize="12" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                <TextBlock Text="设备状态" FontSize="14" Foreground="Gray"/>
                <TextBlock Text="{Binding DeviceStatus}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="运行中" FontSize="12" Foreground="Gray"/>
            </StackPanel>

        </Grid>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Conlist}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"></UniformGrid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <WrapPanel VerticalAlignment="Center">
                            <Button Content="{Binding}" 
                                    Command="{Binding DataContext.OpenDialogCmd,
                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                    CommandParameter="{Binding}">
                            </Button>
                            <Button Content="查看图表"
                                    Command="{Binding DataContext.OpenGraphDialogCmd,
                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}">
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>
        
    </Grid>
</UserControl>
