<Window x:Class="IndustrialMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IndustrialMonitor"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>   

        <StackPanel Grid.Column="0">
            <Button Content="首页看板" Margin="10" 
                    Command="{Binding NavigateCommand}" 
                    CommandParameter="DashboardView"></Button>
            <Button Content="设备通信" Margin="10"
                    Command="{Binding NavigateCommand}" 
                    CommandParameter="DeviceView"></Button>
            <Button Content="实时监控" Margin="10" 
                    Command="{Binding NavigateCommand}" 
                    CommandParameter="MonitorView"></Button>
            <Button Content="报警管理" Margin="10"></Button>
            <Button Content="历史数据" Margin="10"></Button>
            <Button Content="系统设置" Margin="10"></Button>
        </StackPanel>

        <ContentControl Grid.Column="1" 
                        prism:RegionManager.RegionName="ContentRegion"></ContentControl>
    </Grid>
</Window>
